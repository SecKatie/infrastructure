apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: glitchgate-tls
  namespace: glitchgate
spec:
  secretName: glitchgate-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - glitchgate.corp.mulliken.net
