apiVersion: v1
kind: Service
metadata:
  name: librechat
  namespace: librechat
spec:
  selector:
    app.kubernetes.io/name: librechat
  ports:
    - name: http
      port: 80
      targetPort: 3080
