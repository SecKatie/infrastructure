apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: librechat-tls
  namespace: librechat
spec:
  secretName: librechat-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - chat.corp.mulliken.net
