apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: librechat-data
  namespace: librechat
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
