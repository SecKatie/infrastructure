---
# Network configuration defaults
infra_static_ip_gateway: "172.16.10.1"
infra_static_ip_netmask: "255.255.255.0"
infra_static_ip_prefix: 24
infra_static_ip_dns:
  - "172.16.10.1"
  - "1.1.1.1"

# Interface name (will be auto-detected if not specified)
# infra_static_ip_interface: eth0

# Per-host static IP assignments (set in host_vars or inventory)
# infra_static_ip_address: "172.16.10.x"
